{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @multiplayer/db exec prisma generate && pnpm --filter @multiplayer/db exec prisma migrate deploy"
  },
  "deploy": {
    "startCommand": "pnpm --filter server start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
